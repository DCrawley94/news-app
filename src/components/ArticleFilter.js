import React from 'react';

const ArticleFilter = (handleChange) => {
  console.log('in handle change', handleChange);
  return <h5>* * * SORT BY OPTIONS HERE * * *</h5>;
};

export default ArticleFilter;
